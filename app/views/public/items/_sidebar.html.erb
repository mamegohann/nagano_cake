<div>
  <table class="table table-bordered border-dark">
    <thead>ジャンル検索</thead>
    <tbody>
      <% genres.each do |genre| %>
      <tr>
        <td><%= genre.name %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>