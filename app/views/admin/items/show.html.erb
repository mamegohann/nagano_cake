<h2 class="head-line title_h2 text-center m-5"><b>商品詳細</b></h2>
<div class="row">
	<div class="col-6 text-right pr-5">
    <div id="img_fiel">
      <% if @item.image.attached? %>
  	    <%= image_tag @item.image, size: "300x300" %>
  	  <% else %>
  	    <%= image_tag 'no_image', size: "300x300" %>
  	  <% end %>
    </div>
  </div>
  <div class="col-6">
    <div class="field">
	    <p>商品名　　　　　<%= @item.name %></p>
    </div><br>
    <div class="field">
	    <p>商品説明　　　　<%= @item.introduction %></p><br>
    </div>
    <div class="field">
	    <p>ジャンル　　　　<%= @item.genre.name %></p><br>
    </div>
    <div class="field">
	    <p>税込価格　　　　<%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>) 円<br>
	      (税抜価格)
	    </p><br>
    </div>
    <div class="field">
  		<p>
  		  販売ステータス
  	      <% if @item.status == true %>
  	      <b class="text-success">　販売中</b>
  		  <% elsif @item.status == false %>
  			  <b class="text-success">　販売停止中</b>
  		  <% end %>
  		</p>
    </div>
    <div class="actions">
      <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success col-3 m-4 p-3" %>
    </div>
  </div>
</div>